{
  "name": "@eco/vesting",
  "version": "0.0.1",
  "description": "Vesting vault for ECOx",
  "files": [
    "src/**/*.sol"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eco/vesting.git"
  },
  "devDependencies": {
    "prettier": "^2.3.1",
    "prettier-plugin-solidity": "^1.0.0-beta.13"
  },
  "scripts": {
    "lint": "prettier --write src/*.sol && prettier --write src/**/*.sol"
  }
}
