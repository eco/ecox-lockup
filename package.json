{
  "name": "@eco/lockup",
  "version": "0.0.1",
  "description": "Lockup vault for ECOx",
  "files": [
    "src/**/*.sol"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eco/ecox-lockup.git"
  },
  "devDependencies": {
    "prettier": "^2.3.1",
    "prettier-plugin-solidity": "^1.0.0-beta.13"
  },
  "scripts": {
    "lint": "prettier --write src/*.sol && prettier --write src/**/*.sol",
    "remap": "forge remappings > remappings.txt"
  }
}